import { Chat } from "@/components/chat";

function App() {
  return (
    <main className="w-full h-[100svh] bg-slate-100 flex items-center justify-center">
      <Chat />
    </main>
  );
}

export default App;
